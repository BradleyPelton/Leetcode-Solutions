
def largest_sublist(A, length):
    """
        notes
    """
    ourArraysList = []
    for i in range(0,len(A)):
        if i+length > len(a):
            pass
        else:
        

greatest = 0
for i in range(0,len(A))):
for something:
ourArraysList = []
ourArraysList.appen(one of the arrays)



"""
PICTURE
    V = (..........A[i] + A[i+1] + ... + A[i+L-1]) + (A[j] + A[j+1] + ... + A[j+M-1]............)
                             A[0,1,2....] +  A[L] + A[O] + A[M] + a[]