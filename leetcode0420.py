class Solution:
    def maxSumTwoNoOverlap(self, A: List[int], L: int, M: int) -> int:
        """given a natural number LIST A(non neggative integers)
            return the maximum sum of elements in two non-overlapping (contiguous)
            subarrays, which have length L and M.
        """


        def largest_sublist(A, length):
            """
            notes
            """
            ourArraysList = []
            for i in range(0,len(A)-length):
                ourArraysList.append

        greatest = 0
        for i in range(0,len(A))):
            for something:
                ourArraysList = []
                ourArraysList.appen(one of the arrays)
    